<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ethixion Security Checkup</title>
  <style>
    @font-face {
      font-family: 'Myfam';
      src: url('/static/fonts/Poppins-Regular.woff2') format('woff2');
    }
    * {
      font-family: Myfam, sans-serif;
      box-sizing: border-box;
    }
    body {
      margin: 0;
      background: #f4f4f4;
      font-size: 1rem;
      color: #1a1a1a;
      line-height: 1.4;
    }
    .container {
      background-color: transparent;
      color: #1a1a1a;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 2rem 1rem;
      max-width: 750px;
      margin: 0 auto;
      border-radius: 20px;
      text-align: center;
      position: relative;
      width: 90%;
    }
    h1 {
      color: #0a192f;
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
    }
    .subtext {
      color: #444;
      font-size: 1.125rem;
      margin-bottom: 1.5rem;
    }
    .shield-container {
      position: relative;
      width: 8rem;
      height: 8rem;
      margin: 0 auto 1.5rem;
    }
    .shield-svg {
      width: 100%;
      height: 100%;
      animation: pulse-shield 2s infinite;
    }
    @keyframes pulse-shield {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.1); opacity: 0.8; }
    }
    .scan-ring {
      position: absolute;
      top: -0.75rem;
      left: -0.75rem;
      width: 9.5rem;
      height: 9.5rem;
      border-radius: 50%;
      border: 2px solid #0a192f;
      animation: spin 4s linear infinite;
      box-sizing: content-box;
    }
    @keyframes spin {
      0% { transform: rotate(0deg);}
      100% { transform: rotate(360deg);}
    }
    .progress-bar {
      width: 100%;
      height: 0.625rem;
      background: #ddd;
      border-radius: 6px;
      overflow: hidden;
      margin: 1.5rem 0;
    }
    .progress-fill {
      height: 100%;
      width: 0%;
      background: linear-gradient(to right, #0a192f, #64b5f6);
      animation: fillBar 10s linear forwards;
      box-shadow: 0 0 8px #2196f3aa;
    }
    @keyframes fillBar {
      to { width: 100%; }
    }
    .log-box {
      background-color: #fff;
      padding: 1rem 1.25rem;
      border-radius: 10px;
      border: 1px solid #ccc;
      text-align: left;
      max-height: 150px;
      overflow-y: auto;
      font-family: monospace, monospace;
      color: #112240;
      width: 100%;
      max-width: 750px;
      margin: 0 auto;
      box-sizing: border-box;
      font-size: 0.9rem;
      line-height: 1.3;
    }
    .log-line {
      opacity: 0;
      animation: fadeIn 0.6s ease forwards;
      padding: 0.125rem 0;
      white-space: nowrap;
    }
    @keyframes fadeIn {
      to { opacity: 1; }
    }
    .verified-message {
      display: none;
      margin-top: 1.25rem;
      font-size: 20px;
      color: #2e7d32;
      animation: bounce 1s ease infinite alternate;
    }
    .blocked-message {
      display: none;
      margin-top: 1.25rem;
      font-size: 20px;
      color: red;
      animation: bounce 1s ease infinite alternate;
    }
    @keyframes bounce {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }
    .footer-badge {
      width: 100%;
      max-width: 250px;
      color: #fff;
      margin: 1rem auto 0;
      background-color: #0a192f;
      padding: 0.5rem 1rem;
      font-size: 0.85rem;
      border-radius: 30px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      opacity: 0.95;
      transition: transform 0.3s;
      cursor: default;
    }
    .footer-badge:hover {
      transform: scale(1.05);
    }
    @media (max-width: 480px) {
      h1 { font-size: 1.75rem; }
      .subtext { font-size: 1rem; }
      .shield-container { width: 6rem; height: 6rem; margin-bottom: 1rem; }
      .scan-ring { width: 7rem; height: 7rem; top: -0.5rem; left: -0.5rem; }
      .progress-bar { height: 0.5rem; margin: 1rem 0; }
      .log-box { height: 120px; font-size: 0.8rem; }
      .verified-message { font-size: 1.1rem; margin-top: 1rem; }
      .footer-badge { max-width: 200px; font-size: 0.75rem; padding: 0.4rem 0.8rem; }
    }
  </style>
</head>
<body>

<div class="container">
  <h1>Ethixion Security Checkup</h1>
  <p class="subtext">Verifying your request securely...</p>

  <div class="shield-container">
    <svg class="shield-svg" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 2L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-3z" fill="#0a192f" />
      <path d="M12 13l-3-3 1.41-1.41L12 10.17l4.59-4.58L18 7l-6 6z" fill="#ffffff" />
    </svg>
    <div class="scan-ring"></div>
  </div>

  <div class="progress-bar">
    <div class="progress-fill"></div>
  </div>

  <div class="log-box" id="logBox" aria-live="polite" aria-atomic="true">
    🛡️ Ethixion Firewall is Performing a Security Check<br><br>
    {% if logs %}
      {% for log in logs %}
        <div class="log-line">{{ log }}</div>
      {% endfor %}
    {% else %}
      Initializing security scan...<br>
      Please do not close this window or refresh the page.<br>
      You will be automatically redirected once the check is complete.
    {% endif %}
  </div>

  <div class="verified-message" id="successMsg" role="alert">
    Request Verified. Redirecting...
  </div>
  <div class="blocked-message" id="failedMsg" role="alert">
    Request Declined by Ethixion. Redirecting to origin...
  </div>


  <!-- Hidden form -->
  <form id="ethixForm" method="POST" action="{{ redirect_url }}" style="display:none;">
    <input type="hidden" name="ethix_payload" value="{{ body }}">
    <input type="hidden" name="origin_url" value="{{ origin_url }}">
  </form>

  <div class="footer-badge" aria-label="Security badge">
    🔒 Secured by <strong>Ethixion Firewall</strong>
  </div>
</div>

<script>
  const status = {{ status | safe }};
  const originUrl = "{{ origin_url | safe }}";

  if (status === true) {
    setTimeout(() => {
        document.querySelector(".verified-message").style.display = "block";
      }, 3000);

    setTimeout(() => {
      document.getElementById("ethixForm").submit();
    }, 6000);
  } else {
    setTimeout(() => {
        document.querySelector(".blocked-message").style.display = "block";
      }, 3000);
    setTimeout(() => {
      window.location.href = originUrl;
    }, 6000);
  }
</script>
</body>
</html>
